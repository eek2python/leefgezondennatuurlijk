# NOTE: This URLconf is intentionally NOT included in the project URLconf
# (LeefNatuurlijkenGezond/urls.py). All product/category routes live at the
# project root. This file is kept empty to avoid accidental re-inclusion.

from django.urls import path

urlpatterns = [
    # Intentionally empty. Routes are defined at the project level.
]
